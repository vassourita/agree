type User {
  id: ID!
  tag: Int!
  name: String!
  email: String!
  status: String!
  avatarUrl: String!
  createdAt: String!
}

input CreateUserInput {
  name: String!
  email: String!
  password: String!
}

input LoginInput {
  email: String!
  password: String!
}

type LoginResponse {
  token: String!
}

type Mutation {
  createAccount(data: CreateUserInput): User!
  login(data: LoginInput): LoginResponse!
}

type Query {
  me: User!
  user(id: ID!): User
  users: [User!]!
}
