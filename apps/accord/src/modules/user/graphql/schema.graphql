type User {
  id: ID!
  name: String!
  email: String!
  createdAt: String!
}

input CreateUserInput {
  name: String!
  email: String!
}

type Mutation {
  createAccount(data: CreateUserInput): User!
}

type Query {
  users: [User!]!
  user(id: ID!): User!
}
